// Marketing layout — wraps public/conversion pages.
// Navbar and Footer will be added here in Phase 2 when landing page is migrated.
export default function MarketingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
